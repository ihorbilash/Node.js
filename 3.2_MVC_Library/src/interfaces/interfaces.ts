export interface IBook{
    b_id:number,
    title:string,
    link_image: string,
    descr: string,
    year: number,
    count_pages: number,
    count_search: number,
    count_click: number,
    author: string
}
 export interface ICount{
    count:number
}
export interface IB_id{
   b_id:number
}
export interface IA_id{
    a_id:number
 }
